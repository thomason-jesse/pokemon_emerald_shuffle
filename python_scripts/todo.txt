todo:

- ROM changes
    - wally zigzagoon
    - birch zigzagoon
    - [?] Winona bug (in opt)

- generation changes

    - create second network on top of fixed encoder for evolution
        - take in encoded and produce embedding for decoding of evolution
        - also predict evolution type + level + item


    - [?] create network to generate icons/front sprite/back sprite potentially
        - look at weird color palette files; could be a less intensive solution